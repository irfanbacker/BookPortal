# BookPortal
Used books web portal in node.JS

## Dependencies:
* node.Js
* NPM (Node package manager)
* mongoDB

## How to use:
After mongoDB server is started,
In terminal:
1. `npm install`
2. `node server`
  

